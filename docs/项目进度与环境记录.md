# 面试智能助手 (Interview Agent) — 项目进度与环境记录

> 版本：v1.6  
> 日期：2026-04-24  
> 用途：新会话上下文恢复，避免重复踩坑

---

## 一、当前项目状态

### 1.1 已完成 ✅

| 模块 | 状态 | 说明 |
|------|------|------|
| Maven 多模块脚手架 | ✅ 完成 | 5 个子模块 + 父 POM |
| interview-agent-common | ✅ 完成 | Result/ResultCode/BusinessException/GlobalExceptionHandler/CommonConstant |
| interview-agent-model | ✅ 完成 | 4 个 Entity + 4 个 Enum + DTO/VO |
| interview-agent-dao | ✅ 完成 | 4 个 Mapper + schema.sql |
| interview-agent-service | ✅ 完成 | ChatService/RagService/KnowledgeService + 3 个 Tool |
| interview-agent-web | ✅ 完成 | 3 个 Controller + 3 个 Config + 6 个 .st 模板 |
| interview-agent-ui | ✅ 完成 | Vue 3 + Vite + Element Plus + SSE 流式 |
| Docker PostgreSQL | ✅ 运行中 | 端口 5600，schema 自动初始化完成（4 张表） |
| Ollama 模型安装 | ✅ 已安装 | 对话模型: `qwen3:1.7b`（可通过 application.yml 切换），Embedding: `bge-m3` |
| Ollama GPU 加速优化 | ✅ 完成 | Flash Attention + ctx=4096，推理速度 1.6→21.7 tokens/s（见 3.33） |
| Vue 前端启动 | ✅ 已启动 | 端口 3311，已安装依赖 |
| 后端启动验证 | ✅ 通过 | `GET /api/chat/sessions` 返回 200 |
| SSE 流式对话验证 | ✅ 通过 | 前端流式输出正常（已修复 SSE 解析问题） |
| 深度思考模式控制 | ✅ 完成 | 默认关闭，前端开关可控制，需模型支持 |
| Spring AI 版本升级 | ✅ 完成 | 1.0.0 → 1.1.0（支持 OllamaChatOptions.enableThinking/disableThinking） |
| CORS 跨域配置 | ✅ 完成 | WebMvcConfig 配置 allowedOrigins: localhost:5173, localhost:3000 |
| 前端 Markdown 渲染 | ✅ 完成 | markdown-it + highlight.js 代码高亮 |
| 深度思考标签处理 | ✅ 完成 | 前端清理 `<think>...</think>` 标签内容，不显示推理过程 |
| 循环依赖修复 | ✅ 完成 | QuestionGenerateTool 使用 @Lazy + OllamaChatModel（非 ChatClient） |
| 多轮对话支持 | ✅ 完成 | sessionId 关联会话，buildPrompt 加载最近 20 条历史消息 |
| 需求分析文档 | ✅ 完成 | `docs/需求分析.md` |
| 方案设计文档 | ✅ 完成 | `docs/方案设计文档.md` |
| 对话历史删除功能 | ✅ 完成 | 侧边栏 hover 显示"删除"按钮，调用 `DELETE /api/chat/sessions/{id}` |
| 对话历史重命名功能 | ✅ 完成 | 侧边栏 hover 显示"重命名"按钮，行内编辑，调用 `PUT /api/chat/sessions/{id}/rename` |
| 对话历史时间戳分组 | ✅ 完成 | 按"今天 / 过去7天 / 过去30天 / 更早"分组显示 |
| 新增对话按钮 | ✅ 完成 | 左侧菜单栏"智能对话"下方固定的"新增对话"按钮 |
| 流式输出滚动优化 | ✅ 完成 | 用户上滚时不强制跳底，滚回底部后恢复自动跟随 |
| 流式输出 Markdown 实时渲染 | ✅ 完成 | 流式输出时自动补全未关闭的代码块，确保代码高亮实时生效 |
| **RAG 文档导入流程** | ✅ 完成 | PDF(PagePdfDocumentReader)/MD/TXT 多格式解析 + 文件类型识别 |
| **RAG 检索增强流程** | ✅ 完成 | 相似度阈值过滤(0.5) + 领域元数据过滤 + 上下文注入 Prompt |
| **Tool Calling 动态注册** | ✅ 完成 | ChatServiceImpl 中 ragEnabled=true 时注册 KnowledgeSearchTool + QuestionGenerateTool |
| **前端 RAG 开关** | ✅ 完成 | "知识库增强"开关 + tooltip 提示，走 Chat 接口 + ragEnabled 参数 |
| **RAG Prompt 优化** | ✅ 完成 | 检索结果标注来源(领域/标题/段落)，要求标注【补充】内容 |
| **RAG 优化指南文档** | ✅ 完成 | `docs/RAG实现与优化指南.md` |
| **TokenTextSplitter 参数修复** | ✅ 完成 | 修正为5参数构造函数(chunkSize=800, minChunkSizeChars=50, minChunkLengthToEmbed=50, maxNumChunks=10000, keepSeparator=true) |
| **模型名硬编码覆盖修复** | ✅ 完成 | 移除 OllamaChatOptions 中 .model() 硬编码，application.yml 成为模型配置唯一来源，切换模型只需改一处 |
| **RAG 详细日志系统** | ✅ 完成 | logback-spring.xml + [RAG-Search]/[RAG-Prompt]/[RAG-Chat] 标签化日志，可查看完整 Prompt/检索结果/模型响应 |
| **Windows 终端 UTF-8 乱码修复** | ✅ 完成 | .mvn/jvm.config 强制 JVM UTF-8 输出 + 终端 chcp 65001 切换代码页 |

### 1.2 未完成 / 待验证 ❌

| 项目 | 状态 | 说明 |
|------|------|------|
| RAG 全链路运行测试 | ❌ 待运行 | 后端启动后上传 PDF → Chunking → Embedding → 检索 → 生成（代码已编写，编译通过） |
| Tool Calling 运行验证 | ❌ 待运行 | LLM 自主调用工具（代码已编写，编译通过） |
| Knife4j 接口文档 | ❌ 未验证 | http://localhost:8180/doc.html |
| PGVector vector_store 表自动创建 | ❌ 待验证 | Spring AI 自动创建 vector_store 表需确认 |

---

## 二、关键环境信息

### 2.1 开发环境

| 项目 | 值 |
|------|-----|
| 操作系统 | Windows (win32) |
| JDK 路径 | `C:\Users\Zero\.jdks\ms-17.0.15` |
| Maven 路径 | `C:\Program Files\apache-maven-3.9.9` |
| 工作目录 | `d:\桌面\java\interview agent` |
| Docker | Docker Desktop for Windows |
| GPU | NVIDIA GeForce RTX 2060 (6GB VRAM) |
| Ollama 版本 | 0.21.0 |
| Ollama 启动方式 | 必须通过 `start_ollama.bat` 启动（设置 Flash Attention + ctx=4096） |

### 2.2 端口分配

| 服务 | 端口 | 说明 |
|------|------|------|
| Spring Boot 后端 | 8180 | application.yml 配置 |
| Vue 前端 | 3311 | vite.config.js 配置 |
| PostgreSQL | **5600** | 非默认！因 Windows Hyper-V 保留 5432 端口 |

### 2.3 Docker 配置

- **PostgreSQL + PGVector**: `docker-compose.yml`
- 容器内端口 5432 → 宿主机端口 **5600**
- 数据库名: `interview_agent`，用户: `interview_agent`，密码: `interview_agent_2026`
- 初始化 SQL: `interview-agent-dao/src/main/resources/schema.sql` 挂载为 `/docker-entrypoint-initdb.d/01-schema.sql`
- Docker 镜像源配置: `C:\Users\Zero\.docker\daemon.json`

---

## 三、踩坑记录（重要！）

### 3.1 Docker 镜像拉取失败

**问题**：ustc/baidu/registry 镜像源均已失效  
**解决**：`daemon.json` 已更新为可用源：
```json
{
  "registry-mirrors": [
    "docker.1ms.run",
    "docker.xuynuan.me", 
    "docker.m.daocloud.io"
  ]
}
```

### 3.2 PostgreSQL 端口 5432 被 Windows Hyper-V 占用

**问题**：Windows Hyper-V 保留端口范围 4539-5557，导致 5432 无法绑定  
**解决**：改为端口 5600，同时修改了 `docker-compose.yml` 和 `application.yml`

### 3.3 Maven 依赖构建顺序

**问题**：单独运行 web 模块会报依赖找不到  
**解决**：必须从根目录先执行 `mvn clean install -DskipTests`，再启动 web 模块

### 3.4 Spring AI 1.0.0 GA 依赖名称变更

**问题**：GA 版本重命名了 starter artifact  
**解决**：
- `spring-ai-ollama-spring-boot-starter` → `spring-ai-starter-model-ollama`
- `spring-ai-pgvector-store-spring-boot-starter` → `spring-ai-starter-vector-store-pgvector`

### 3.5 pgvector0 依赖冲突

**问题**：Alibaba Maven 仓库找不到 `pgvector0` 依赖  
**解决**：从 dao/pom.xml 和父 POM 的 dependencyManagement 中移除 pgvector0，由 Spring AI starter 自动管理

### 3.6 循环依赖

**问题**：SpringAiConfig 创建 ChatClient → OllamaChatModel 自动发现 @Tool Bean → QuestionGenerateTool 注入 ChatClient → 形成循环  
**解决**：
1. SpringAiConfig 中 ChatClient 不注册任何 Tool（Tool 在 Service 层通过 `.tools()` 动态注册）
2. QuestionGenerateTool 改为注入 `OllamaChatModel`（而非 ChatClient），并使用 `@Lazy` 延迟注入
3. 循环链：`OllamaChatModel` → `ToolCallingManager` → `QuestionGenerateTool` →(lazy) `OllamaChatModel`

### 3.7 Mapper 扫描不到

**问题**：启动类在 `com.interview.agent.web`，Mapper 在 `com.interview.agent.dao.mapper`  
**解决**：在 `InterviewAgentApplication.java` 添加 `@MapperScan("com.interview.agent.dao.mapper")`

### 3.8 MyBatis-Plus 3.5.9 无 PaginationInnerInterceptor

**问题**：该类在 3.5.9 中不存在  
**解决**：简化 MyBatisPlusConfig，移除分页插件

### 3.9 前端 SSE 流式输出显示原始 `data:` 前缀

**问题**：前端使用 `fetch` + `ReadableStream` 直接读取 SSE 响应，未解析 SSE 协议格式，导致页面显示 `data:你好` 而非 `你好`  
**原因**：SSE 标准格式为 `data:内容\n\n`，前端直接拼接原始 chunk  
**解决**：添加 `parseSSEChunk()` 函数，逐行解析 SSE 数据并去掉 `data:` 前缀

### 3.10 后端历史消息重复

**问题**：`chatStream/chat` 方法先保存用户消息到数据库，`buildPrompt` 查询历史时查出该消息，又手动添加一次 `UserMessage`，导致当前用户消息在 prompt 中重复  
**解决**：调整执行顺序，先构建 Prompt，再保存用户消息

### 3.11 Qwen3.5 深度思考模式浪费大量时间

**问题**：Qwen3.5 默认启用深度思考（thinking），导致响应极慢，且思考过程输出到前端  
**原因**：Ollama 0.12+ 中思考型模型自动启用思考模式；`/no_think` 对 Qwen3.5 不生效  
**解决**：
1. 升级 Spring AI 从 1.0.0 到 1.1.0（1.0.x 不支持 `think` 参数）
2. 使用 `OllamaChatOptions.builder().disableThinking()` 默认关闭深度思考
3. 前端添加"深度思考"开关，根据模型是否支持来控制可用性
4. 支持深度思考的模型列表：qwen3、qwen3.5、deepseek-r1、deepseek-v3.1

### 3.12 Spring AI 1.0.0 不支持 Ollama `think` 参数

**问题**：Spring AI 1.0.0 的 `OllamaOptions` / `OllamaApi.ChatRequest` 均无 `think` 字段  
**解决**：升级到 Spring AI 1.1.0，使用 `OllamaChatOptions.builder().enableThinking()/.disableThinking()`

### 3.13 CORS 跨域请求被拦截

**问题**：前端（localhost:3311）请求后端（localhost:8180）被浏览器 CORS 策略拦截  
**解决**：在 `WebMvcConfig.java` 中配置 `allowedOrigins("http://localhost:5173", "http://localhost:3000")`；前端通过 vite proxy 代理 `/api` 到后端，开发模式下无需跨域

### 3.14 DeepSeek 模型 `<think>` 标签泄露到前端

**问题**：DeepSeek 等支持深度思考的模型会在输出中包含 `<think>...</think>` 推理过程标签，直接显示给用户影响体验  
**解决**：前端 `renderMarkdown()` 函数中添加正则 `text.replace(/<think>[\s\S]*?<\/think>/g, '').trim()` 清理推理内容后再渲染

### 3.15 前端 Markdown 渲染和代码高亮

**问题**：AI 返回的 Markdown 格式内容（代码块、列表、标题等）在前端显示为纯文本  
**解决**：
1. 安装 `markdown-it` + `highlight.js` 依赖
2. 配置 markdown-it 的 highlight 回调使用 highlight.js 进行代码高亮
3. 引入 `highlight.js/styles/github.css` 样式
4. 添加 `word-wrap: break-word` 防止长文本溢出布局

### 3.16 chat_session 表缺少 title 字段

**问题**：对话会话表原本没有 `title` 字段，无法支持重命名功能，前端显示只能用 `domain`  
**解决**：
1. `schema.sql` 中 `chat_session` 表新增 `title VARCHAR(255)` 列
2. `ChatSession` Entity 和 `ChatSessionVO` 新增 `title` 字段
3. 对运行中的数据库执行 `ALTER TABLE chat_session ADD COLUMN IF NOT EXISTS title VARCHAR(255)`
4. 新增 `PUT /api/chat/sessions/{sessionId}/rename` 接口

### 3.17 流式输出时强制自动滚动，用户无法查看历史

**问题**：SSE 流式输出期间，每次收到新 chunk 都调用 `scrollToBottom()` 强制跳到底部，用户无法上滚查看之前的消息  
**解决**：
1. 添加 `isUserAtBottom` 状态，通过 `@scroll` 事件检测用户是否在底部附近（阈值 80px）
2. `scrollToBottom(force)` 仅在 `isUserAtBottom=true` 或 `force=true` 时自动滚动
3. 用户主动上滚时不打断，滚回底部后恢复自动跟随
4. 用户发送新消息时，强制 `isUserAtBottom = true` + `scrollToBottom(true)`

### 3.18 流式输出时 Markdown 代码高亮不生效

**问题**：SSE 流式输出过程中，Markdown 渲染为纯文本，代码块没有语法高亮，只有刷新页面后才显示正确  
**原因**：流式输出时代码块可能未关闭（如 ` ```java ` 还没有结束的 ` ``` `），`markdown-it` 无法识别为代码块，`hljs.highlight` 不触发  
**解决**：
1. 每条消息新增 `streaming` 标记，流式输出时为 `true`，结束时为 `false`
2. `renderMarkdown(text, streaming)` 在流式模式下统计 ` ``` ` 出现次数，奇数则自动追加 ` \n``` ` 补全未关闭的代码块
3. 这样代码块始终能被 `markdown-it` 正确解析并触发 `hljs.highlight` 实时高亮

### 3.19 侧边栏操作按钮太小难以点击

**问题**：对话历史的编辑和删除使用 14px 的 `el-icon`，太小且无文字说明，用户难以发现和点击  
**解决**：改用 `el-button`（text 类型）同时显示图标 + 文字（"重命名"、"删除"），增大可点击区域和视觉辨识度

### 3.20 删除/重命名会话返回 403 错误

**问题**：前端调用 `DELETE /api/chat/sessions/{id}` 和 `PUT /api/chat/sessions/{id}/rename` 返回 403  
**原因**：`application.yml` 中配置了 MyBatis-Plus 全局逻辑删除（`logic-delete-field: status`），导致所有 `mapper.delete()` 被转为 `UPDATE SET status='DELETED'`，但 `chat_session` 和 `chat_message` 表没有 `status` 字段，SQL 执行失败  
**解决**：
1. 移除 `application.yml` 中的全局逻辑删除配置
2. 在 `KnowledgeDocument` 实体的 `status` 字段上单独添加 `@TableLogic(value = "ACTIVE", delval = "DELETED")` 注解
3. 这样只有 `knowledge_document` 表使用逻辑删除，其他表正常物理删除

### 3.21 CORS 403：前端端口未加入白名单

**问题**：删除/重命名会话仍然返回 403 `Invalid CORS request`  
**原因**：前端运行在 `localhost:3311`，但 `WebMvcConfig.java` 的 `allowedOrigins` 只有 `5173` 和 `3000`；DELETE/PUT 等非简单请求触发 OPTIONS 预检，预检被 CORS 拦截  
**解决**：在 `allowedOrigins` 中添加 `"http://localhost:3311"`

### 3.22 流式输出 Markdown 样式不实时渲染（Vue 响应式问题）

**问题**：SSE 流式输出时，AI 回复显示为原始 Markdown 文本（如 `### 标题`、`- 列表`），刷新页面后才正确渲染为 HTML 样式  
**原因**：之前使用 `v-html="renderMarkdown(msg.content, msg.streaming)"` 在模板中调用函数，Vue 的响应式系统对深层属性变化（`msg.content`）的追踪不可靠，导致 `v-html` 不更新  
**解决**：
1. 消息对象新增 `renderedHtml` 属性，存储渲染后的 HTML
2. `v-html` 直接绑定 `msg.renderedHtml`，不再调用函数
3. 每次内容更新时，通过 `updateMessageContent()` 函数**替换整个数组元素**（`messages.value[msgIndex] = { ...msg, content, streaming, renderedHtml }`），确保 Vue 检测到引用变化
4. `renderToHtml()` 在 `streaming=true` 时自动补全未关闭的代码块

### 3.23 流式输出 Markdown 样式仍不渲染（Vue 虚拟 DOM diff 问题）

**问题**：即使改用 `msg.renderedHtml` 属性 + 替换数组元素的方式，SSE 高频更新时 Vue 的虚拟 DOM diff 仍然可能跳过 `v-html` 的实际 DOM 更新，导致 Markdown 始终显示为原始文本  
**解决**：
1. 新增 `messageRefs` 对象，存储每个消息 DOM 元素的引用
2. `updateMessageContent()` 中除了更新响应式数据外，还通过 `nextTick` **直接操作 DOM innerHTML**
3. 比对 `el.innerHTML !== renderedHtml` 后才赋值，避免不必要的 DOM 操作
4. 双保险：响应式 + 直接 DOM 操作，确保无论如何都能正确刷新

### 3.24 SSE 流首条消息携带 sessionId JSON 污染内容

**问题**：后端每次流式请求（不仅是首次）都在流的第一条发送 `{"sessionId":"xxx"}`，前端只在第一次对话时过滤了它；后续对话中该 JSON 字符串混入了 AI 回复内容并直接显示出来  
**原因**：`ChatController.chatStream()` 使用 `Flux.concat(sessionIdEvent, contentStream)` 将 sessionId 作为第一个事件发出，但前端之前仅在 `!currentSessionId.value` 时才提取和移除它  
**解决**：
1. 新增 `extractSessionId(buffer)` 函数，**始终从 buffer 中检测并移除** sessionId JSON 行（不管是否已有会话ID）
2. 改进 `parseSSEChunk()` 函数，在解析时也过滤掉 `{...sessionId...}` 格式的数据
3. 双重过滤确保 sessionId 不会混入 AI 回复内容

### 3.25 SSE 流式输出 Markdown 标题不渲染（换行符丢失 — 根因修复）

**问题**：SSE 流式输出时，`**加粗**` 能渲染但 `### 标题` 不能，显示为原始文本  
**根因**：`parseSSEChunk()` 函数用 `result += data` 直接拼接每条 `data:` 行的内容，**丢失了行间换行符**

后端 Spring WebFlux `Flux<String>` 输出格式：
```
data:你好我是助手。
data:
data:### 面试题解析
data:
data:**参考答案**
```

错误拼接为：`你好我是助手。### 面试题解析**参考答案**`
→ `###` 不在行首 → markdown-it 不识别为标题 ❌
→ `**` 在段落中间 → 恰好能被 inline 解析器识别 ✅

**参考主流方案（ChatGPT / Streamdown / streaming-markdown）的核心做法**：
1. **保留原始文本结构**：每个 data: 行用 `\n` 连接，不丢失换行
2. **增量 DOM 渲染**：直接操作 innerHTML + Vue 响应式双保险
3. **容忍不完整 Markdown**：流式时自动补全未关闭的代码块

**修复**：
1. 重写 `parseSSEChunk()` → 改名为 `parseSSEDataLines()`，使用 `parts.join('\n')` 保留换行
2. 清理冗余逻辑，代码结构更清晰
3. 参考了 [streaming-markdown](https://github.com/thetarnav/streaming-markdown) 和 [Streamdown](https://juejin.cn/post/7568796644347723776) 的实现思路

### 3.26 SSE 流式输出文字内容丢失（buffer 清空导致截断）— 最终根因修复

**问题**：流式输出时不仅 Markdown 样式不渲染，**连文字内容本身都在丢失**
- 流式输出时显示：`封装数据捆绑，暴露必要，。限制对象的直接数据完整性，降低，代码修改`
- 刷新后完整：`封装是将数据与方法捆绑，仅暴露必要接口，隐藏内部实现细节。它通过限制外部对对象的直接访问，保护数据完整性，降低耦合度，便于代码维护和修改。`

**根因**：`readSSEStream()` 中每次解析后直接 `buffer = ''` 清空整个缓冲区！
```
while 循环中：
  buffer += decoder.decode(value, { stream: true })  // 追加新数据
  const parsed = parseSSEDataLines(buffer)             // 提取 data: 内容
  assistantContent += parsed                           // 累加到结果
  buffer = ''  // ← 致命错误！清空了整个 buffer，丢弃未完成的数据行！
```
网络传输中一个 TCP 包可能切断在任意位置（比如 `data:封装是` 被切成两包），`buffer = ''` 直接把后半部分丢掉了。

**参考主流方案（CSDN / OpenAI SDK / Vercel AI SDK 的标准模式）重写**：
1. **`extractSSEDatas(buffer)`** — 只从 buffer 中提取已完成的 `data:` 行；如果 buffer 不以 `\n` 结尾，说明最后一行未完成，保留在 `remainingBuffer`
2. **TextDecoder `stream:true`** — 正确处理被切断的多字节 UTF-8 字符（中文 3 字节）
3. **流结束时 `decoder.decode(new Uint8Array(0), { stream: false })`** — 刷新 TextDecoder 内部缓冲区
4. **空行保留为换行符** — 确保 `###` 在行首、段落之间有正确间隔

### 3.27 TokenTextSplitter 构造函数参数错误

**问题**：原代码使用 5 个参数但含义错误（如将 `minChunkSizeChars` 传了 50，`maxNumChunks` 传了 100，`maxChunkSizeChars` 传了 true），且 Spring AI 1.1.0 的 `TokenTextSplitter` 全参构造函数只有 5 个参数（无 `punctuationMarks`）  
**解决**：
1. 修正为正确的 5 参数构造函数：`TokenTextSplitter(chunkSize=800, minChunkSizeChars=50, minChunkLengthToEmbed=50, maxNumChunks=10000, keepSeparator=true)`
2. chunkSize 从 500 调整为 800，更适合中文面试知识文档
3. `minChunkLengthToEmbed` 与 `minChunkSizeChars` 保持一致为 50

### 3.28 RagServiceImpl 仅支持 TextReader（不支持 PDF）

**问题**：原 `importDocument()` 方法仅使用 `TextReader`，无法解析 PDF 文件  
**解决**：
1. 新增 `parseDocument()` 方法，根据文件类型分发：
   - PDF → `PagePdfDocumentReader`（Apache PdfBox，按页提取）
   - MD/TXT → `TextReader`（纯文本直接读取）
   - 其他 → 降级为 TextReader
2. `KnowledgeController.uploadDocument()` 中识别文件后缀，传入 `fileType` 参数
3. 文件类型信息写入 `knowledge_document.file_type` 字段和向量元数据

### 3.29 RAG 两种模式统一

**问题**：前端 RAG 开关调用 `/api/rag/chat/stream` 独立接口，而非通过 Chat 接口的 Tool Calling  
**解决**：
1. 修改 `streamRagChat()` 改为调用 `/api/chat/stream?ragEnabled=true`
2. 后端 `ChatServiceImpl` 检测 `ragEnabled=true` 时，通过 `.tools(knowledgeSearchTool, questionGenerateTool)` 动态注册 Tool
3. LLM 自主决定是否调用 KnowledgeSearchTool，更智能（而非强制先检索再生成）

### 3.30 文件上传路径 FileNotFoundException

**问题**：`UPLOAD_DIR = "uploads/"` 是相对路径，`file.transferTo()` 基于 Tomcat 临时目录解析，而 `Files.createDirectories()` 基于 JVM 工作目录解析，两者不一致导致目录找不到  
**解决**：在 `application.yml` 中配置 `app.upload.dir: ${user.dir}/uploads`，Controller 通过 `@Value` 注入并调用 `.toAbsolutePath()`

### 3.31 bge-m3 Embedding 全部返回 NaN（HTTP 500）

**问题**：Ollama bge-m3 对所有文档做 Embedding 都报 `HTTP 500 - {"error":"failed to encode response: json: unsupported value: NaN"}`  
**根因**（GitHub Issue [#13572](https://github.com/ollama/ollama/issues/13572)）：llama.cpp 的 **Flash Attention 对 BERT 类模型有 F32→F16 精度溢出 bug**。当 K/V 张量为 F32 时被强制转换为 F16，F16 最大值约 65504，中间值超过此限制溢出为 Inf → softmax 传播后变 NaN → JSON 无法序列化  
**解决**：设置环境变量 `OLLAMA_FLASH_ATTENTION=false`（用户级），重启 Ollama 服务即可  
**注意**：
- 设 `0` 无效，必须用 `false`
- 所有基于 BERT 架构的嵌入模型（bge-m3、intfloat-multilingual-e5-large 等）均受影响
- 替代方案：换用非 BERT 架构的嵌入模型如 `nomic-embed-text`、`mxbai-embed-large`

### 3.32 向量存储日志统计 bug — 成功数与总数混淆

**问题**：代码中分批/逐条失败后仍打印"向量存储完成: 101 个分块已写入 PGVector"，实际 0 个成功。原因是日志写的是 `validChunks.size()` 而非实际成功数，且全部失败时仍写入 `knowledge_document` 表（status=ACTIVE）  
**解决**：
1. 增加 `successCount` / `failCount` 计数器，逐条调用 `vectorStore.add(List.of(doc))`
2. 全部失败（`successCount == 0`）时抛异常，不写入数据库
3. `knowledge_document.chunk_count` 记录实际成功入库数
4. 日志明确输出"成功 X/Y 个分块"

### 3.33 Ollama GPU 不加速 — CPU 100% 而 GPU 空闲

**问题**：Ollama 运行 `qwen3.5:4b` 模型时 CPU 占用 100%，GPU 占用不到 10%，推理速度仅 1.6 tokens/s，导致智能对话几乎不可用（请求超时或极慢）。而 `qwen3:1.7b` 模型却能正常使用 GPU  
**根因**：Ollama 0.21.0 新引擎对 `qwen3.5:4b` 的 compute graph（计算图）占用 **4.3 GiB** 显存，RTX 2060 仅 6GB 总显存，计算图吃掉大部分后只剩 ~700MB 给模型权重，导致 **32/33 层被迫跑在 CPU 上**（仅 1 层在 GPU）  
**解决**：
1. **开启 Flash Attention**：设置环境变量 `OLLAMA_FLASH_ATTENTION=1`，计算图从 4.3 GiB → **727 MiB**（降 83%）
2. **降低上下文长度**：设置 `OLLAMA_CONTEXT_LENGTH=4096`（从默认 8192 降低），进一步减少 KV 缓存占用
3. **Spring AI 配置同步**：`application.yml` 中添加 `num-ctx: 4096`，确保请求参数与服务端一致
4. **创建启动脚本**：`start_ollama.bat` 在启动 Ollama 前设置环境变量（Ollama 系统托盘启动不会读取 setx 设置的变量）
5. **关闭占用显存的程序**：如 MuMu 模拟器等

**优化效果**：

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| GPU 层数 | 2/33 (6%) | 32/33 (97%) |
| 推理速度 | 1.6 tokens/s | 21.7 tokens/s (13.5倍) |
| 模型启动时间 | 58秒 | 4.4秒 |
| 计算图占用(CUDA) | 4.3 GiB | 727 MiB |

**注意事项**：
- `OLLAMA_FLASH_ATTENTION` 对 BERT 类嵌入模型（如 bge-m3）有精度溢出 bug（见 3.31），但对话模型不受影响
- `OLLAMA_CONTEXT_LENGTH` 是 Ollama 服务端默认值，Spring AI 请求中的 `num-ctx` 参数优先级更高，会覆盖服务端默认值
- 环境变量通过 `setx` 永久设置，但 Ollama 系统托盘应用启动时不会读取，必须通过 `start_ollama.bat` 或在 PowerShell 中手动设置后启动

### 3.34 模型名硬编码覆盖 application.yml 配置

**问题**：在 `application.yml` 中将模型改为 `qwen3:1.7b`，但实际运行的仍然是 `qwen3.5:4b`  
**根因**：`CommonConstant.DEFAULT_CHAT_MODEL = "qwen3.5:4b"`，三个地方在构建 `OllamaChatOptions` 时显式调用了 `.model(CommonConstant.DEFAULT_CHAT_MODEL)`，**覆盖了 application.yml 的配置**：
1. `ChatServiceImpl.buildPrompt()` — 对话 Prompt 构建
2. `RagServiceImpl.buildRagPrompt()` — RAG Prompt 构建
3. `QuestionGenerateTool.generateQuestions()` — Tool 内部调用

**解决**：
1. 移除所有 `OllamaChatOptions.builder().model(CommonConstant.DEFAULT_CHAT_MODEL)` 中的 `.model()` 调用
2. 改用 `@Value("${spring.ai.ollama.chat.options.model}")` 注入模型名，用于 `session.setModelName()` 等需要记录模型名的场景
3. **application.yml 成为模型配置的唯一来源**，以后换模型只需改一处

### 3.35 RAG 调试日志缺失 — 无法看到 Prompt 和检索结果

**问题**：RAG 模块已编写完成但无法验证，因为缺少关键日志：不知道发送了什么 Prompt、检索到哪些文档、模型返回了什么  
**解决**：
1. 新增 `logback-spring.xml` 日志配置文件（控制台彩色输出 + 文件滚动日志 `%TMP%/interview-agent.log`）
2. `ChatServiceImpl` 添加标签化日志：`[Prompt]`、`[Chat]`、`[ChatStream]`
3. `RagServiceImpl` 添加标签化日志：`[RAG-Search]`（检索参数+每条结果详情+相似度）、`[RAG-Prompt]`（上下文长度+完整 System Prompt）、`[RAG-Chat]`/`[RAG-Stream]`（模型响应）
4. `application.yml` 开启 Spring AI DEBUG 日志：`org.springframework.ai.ollama`、`org.springframework.ai.chat.client`、`org.springframework.ai.vectorstore`
5. 日志分级：INFO 输出摘要信息（检索命中数、响应长度），DEBUG 输出完整内容（Prompt、检索结果全文、模型响应全文）

**日志标签速查**：

| 标签 | INFO 级别 | DEBUG 级别 |
|------|-----------|------------|
| `[Prompt]` | 模型/领域/历史数/消息数 | System Prompt 完整内容 |
| `[RAG-Search]` | 检索参数/命中数/每条结果摘要 | 每条结果完整内容 |
| `[RAG-Prompt]` | similarDocs数/上下文长度/历史数 | RAG上下文+System Prompt完整内容 |
| `[Chat]/[ChatStream]` | 调用开始/完成/响应长度 | LLM完整响应内容 |
| `[RAG-Chat]/[RAG-Stream]` | 调用开始/完成/响应长度 | LLM完整响应内容 |

### 3.36 Windows 终端中文日志乱码

**问题**：PowerShell 中启动 Spring Boot 后，日志中的中文显示为乱码（如 `娴ｇ姵妲告稉锟芥担宥勭瑩` 等）  
**根因**：Windows PowerShell 默认代码页为 **936 (GBK)**，而 Java 程序输出 **UTF-8**，编码不匹配  
**解决**：
1. 创建 `.mvn/jvm.config`，强制 JVM 使用 UTF-8 编码输出：
   ```
   -Dfile.encoding=UTF-8
   -Dsun.stdout.encoding=UTF-8
   -Dsun.stderr.encoding=UTF-8
   -Dconsole.encoding=UTF-8
   ```
2. 终端启动前执行 `chcp 65001` 切换到 UTF-8 代码页
3. 一劳永逸方案：Windows 设置 → 时间和语言 → 管理语言设置 → 更改系统区域设置 → 勾选"Beta: 使用 Unicode UTF-8 提供全球语言支持" → 重启

---

## 四、核心代码文件索引

### 4.1 后端关键文件

| 文件 | 路径 |
|------|------|
| 父 POM | `pom.xml` |
| 应用配置 | `interview-agent-web/src/main/resources/application.yml` |
| 启动类 | `interview-agent-web/src/main/java/com/interview/agent/web/InterviewAgentApplication.java` |
| Spring AI 配置 | `interview-agent-web/src/main/java/com/interview/agent/web/config/SpringAiConfig.java` |
| CORS 配置 | `interview-agent-web/src/main/java/com/interview/agent/web/config/WebMvcConfig.java` |
| ChatController | `interview-agent-web/src/main/java/com/interview/agent/web/controller/ChatController.java` |
| RagController | `interview-agent-web/src/main/java/com/interview/agent/web/controller/RagController.java` |
| KnowledgeController | `interview-agent-web/src/main/java/com/interview/agent/web/controller/KnowledgeController.java` |
| ChatService | `interview-agent-service/src/main/java/com/interview/agent/service/chat/ChatServiceImpl.java` |
| RagService | `interview-agent-service/src/main/java/com/interview/agent/service/rag/RagServiceImpl.java` |
| KnowledgeService | `interview-agent-service/src/main/java/com/interview/agent/service/knowledge/KnowledgeServiceImpl.java` |
| KnowledgeSearchTool | `interview-agent-service/src/main/java/com/interview/agent/service/tool/KnowledgeSearchTool.java` |
| QuestionGenerateTool | `interview-agent-service/src/main/java/com/interview/agent/service/tool/QuestionGenerateTool.java` |
| Prompt 模板目录 | `interview-agent-web/src/main/resources/prompts/` |
| 数据库 Schema | `interview-agent-web/src/main/resources/sql/schema.sql` |
| 日志配置 | `interview-agent-web/src/main/resources/logback-spring.xml` |
| JVM 编码配置 | `.mvn/jvm.config` |
| RAG 实现与优化指南 | `docs/RAG实现与优化指南.md` |

### 4.2 前端关键文件

| 文件 | 路径 |
|------|------|
| Vite 配置 | `interview-agent-ui/vite.config.js` |
| 路由 | `interview-agent-ui/src/router/index.js` |
| HTTP 请求封装 | `interview-agent-ui/src/utils/request.js` |
| 聊天页面 | `interview-agent-ui/src/views/ChatView.vue` |
| 知识库页面 | `interview-agent-ui/src/views/KnowledgeView.vue` |

---

## 五、下一步开发计划

按优先级排序：

1. ~~**验证基础对话**~~ → ✅ 已完成
2. ~~**前后端联调**~~ → ✅ SSE 流式对话已通过
3. ~~**CORS / Markdown / 循环依赖 / 多轮对话修复**~~ → ✅ 已完成
4. ~~**前端交互优化（对话历史 CRUD / 时间分组 / 新增对话 / 滚动 / Markdown 实时渲染）**~~ → ✅ 已完成
5. ~~**RAG 代码实现**~~ → ✅ PDF解析 + Chunking + 向量检索 + Tool Calling + 前端RAG开关
6. **RAG 全链路运行测试** → 启动后端 → 上传 PDF → Embedding → 检索 → 增强回答
7. **Tool Calling 运行验证** → 观察日志中 LLM 是否自主调用 KnowledgeSearchTool
8. **知识库管理优化** → 文档删除时同步清理 PGVector 中的向量数据
9. **UI 美化 & 交互优化**（进一步打磨细节）
10. **RAG 进阶优化** → 混合检索(Dense+Sparse)、Re-ranking、查询改写
11. **Phase 3 进阶功能**（模拟面试、多模型切换等）

---

## 六、给新会话的 Prompt 模板

新开会话时，可以复制以下内容作为首条消息：

```
我正在开发一个面试智能助手项目，项目已经完成了基础脚手架搭建，需要继续开发。

请先阅读以下文件了解项目全貌：
1. docs/需求分析.md — 需求定义
2. docs/方案设计文档.md — 技术方案
3. docs/项目进度与环境记录.md — 当前进度、环境信息、踩坑记录

项目工作目录：d:\桌面\java\interview agent

当前待完成的任务：[在这里写你要做什么]
```
